import type { Config } from "vike/types";
import vikeReact from "vike-react/config";

export default {
    title: "Kyle Dunston",
    description: "Kyle Dunston Portfolio",
    extends: [vikeReact],
    prerender: true,
} satisfies Config;
